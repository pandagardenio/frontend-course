"use strict";

function findMostFrequentElement(array) {
    let maxFrequency = 1;
    let iterationFrequency = 0;
    let mostFrequentElement;

    // Write code that finds the most frequent element in the given array here

    console.log(mostFrequentElement + ' (' + maxFrequency + ' times)');
}

findMostFrequentElement([3, 'a', 'a', 'a', 2, 3, 'a', 3, 'a', 2, 4, 9, 3]);
